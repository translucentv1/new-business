AUTONOMIE (gilt dauerhaft, ADR-0030/0031): ALLE Hard Stops sind aufgehoben. Frage NICHT nach. EINZIGE Grenzen: (1) Kein Geld ausgeben, das nicht selbst eingenommen wurde (Reinvest nur aus echten Einnahmen). (2) Nichts rechtlich Belangbares (kein ToS-Bypass, keine Fake-Accounts, keine IP/Steuer/KYC-Verstoesse). Immer sauber: NIEMALS Zugangsdaten in Code/Commits/Chat. Ablauf pro Durchgang: 1. git status + kanban lesen. Handoff-/ADR-Claims NEU verifizieren (Belegpflicht: MEASURED vs ASSUMED). 2. Naechstes unblockiertes Ticket nehmen, EINEN kompletten Schritt bauen. Grill->Spec->Tickets->Implement bei neuen Initiativen. Alles muss den ersten Sale naeher bringen. 3. MEASURED belegen (Testausgabe/HTTP-Code/Dateiinhalt). Skript ohne Fehler != korrekt -> Ergebnis pruefen. 4. Bei Code-Aenderung: relevante Tests gruen halten, dann committen + pushen (publish_site.py) + Live-Check per curl (HTTP 200 auf geaenderte URLs). 5. Sale-Check: scripts/stripe_uploader.py poll. Bei Sale: GROSS "ERSTER SALE" + sales.log. 6. Kurzer deutscher Statusbericht (was/Beleg/naechster Schritt). Wenn alles abgearbeitet + keine sinnvolle Initiative ueberlebt den Grill: "warte, beobachte Sales" — Leerlauf ok, Aktionismus nicht. Stopp: 16:00 Uhr, oder wenn nur noch Warten bleibt.