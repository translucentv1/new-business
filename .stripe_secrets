# Stripe Secrets — NUR den Live-Key einfügen, dann Datei bleibt lokal (gitignored)
# NICHT committen. Hermes liest STRIPE_SECRET_KEY aus dieser Datei oder aus der Env.
# Format OHNE Anführungszeichen, OHNE Leerzeichen um das =-Zeichen.

STRIPE_SECRET_KEY=sk_live_XXX_FÜLLEN_SOBALD_VERFÜGBR

# Optional: Webhook-Secret (für HMAC-Verifkation in app.py /webhook Route)
# Holt man aus Stripe Dashboard -> Developers -> Webhooks -> [Endpoint] -> Signing secret
STRIPE_WEBHOOK_SECRET=whsec_XXX_OPTIONAL
